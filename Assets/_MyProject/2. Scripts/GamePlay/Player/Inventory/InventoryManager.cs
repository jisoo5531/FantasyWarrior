using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventoryManager : MonoBehaviour
{    
    public void GetItem_InsertDatabase()
    {
        //string query =
        //    $"INSERT INTO Inventory (user_id, item_id, quantity)\n" +
        //    $"VALUES (1, )"
    }
}
